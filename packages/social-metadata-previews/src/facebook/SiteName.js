/* External dependencies */
import styled from "styled-components";

/**
 * A paragraph styled to display a site name.
 *
 * @type {ReactComponent}
 */
const SiteName = styled.p`
	color: #606770;
	font-size: 12px;
	line-height: 11px;
	text-transform: uppercase;
	overflow: hidden;
	text-overflow: ellipsis;
	white-space: nowrap;
`;

export default SiteName;
